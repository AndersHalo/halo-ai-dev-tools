{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-acli.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If the last command was an acli jira command that failed, suggest: 1) check auth with 'acli jira auth login', 2) verify ticket key, 3) check .jira-config.json for correct project key."
          }
        ]
      }
    ]
  }
}
